# wechat-use v1.12 — Orchestrate Protocol

**Audience**: 任何想让自家 SaaS 接入用户本机微信、但用户 Mac 没公网的开发者。

**Pattern**: outbox + webhook（Mac 全 outbound 调用，NAT-friendly）。

---

## 0. 角色 & 数据流

```
┌────────────────────────────────────┐
│ 你的 SaaS (CF Worker / VPS / etc.) │
│                                     │
│  - bot_outbox 表（你存）            │
│  - 4 个 endpoint（你实现）：         │
│    GET  /api/wechat-outbox/claim   │
│    POST /api/wechat-outbox/:id/done│
│    POST /api/wechat-outbox/:id/fail│
│    POST /api/wechat-inbound        │
│                                     │
│  - 租户各自一个 SaaS bearer token │
└────────────────────────────────────┘
                ▲ poll / push
                │ HTTPS + Bearer
                │
┌────────────────────────────────────┐
│ 租户 Mac (wechat-use v1.12)     │
│                                     │
│  wechat-use orchestrate                 │
│    --outbox-url=<your saas base>   │
│    --webhook-url=<your saas inbound>│
│    --bearer=<token-per-tenant>     │
│                                     │
│  - poll outbox → 调本机 :18402 send│
│  - 订 SSE :18400 → POST webhook    │
└────────────────────────────────────┘
```

**关键不变量**：
- Mac 全部 outbound HTTP（NAT-friendly，租户不需要域名 / 公网 / cloudflared）
- SaaS 是真相源（state lives in your D1 / Postgres / KV，不存在 Mac 上）
- Bearer token 跟租户一对一，scope 只能操作该 tenant 的 outbox + inbound

---

## 1. 鉴权

每次 SaaS 给一个租户发激活/接入凭据时，签发**一次性 SaaS-side bearer token**：

```
HTTP Header:
  Authorization: Bearer <saas-issued-token>
```

**禁止**复用 wechat-use 的 `wxp_tok_*` user_token —— 那是 wechat-use 跟激活用户之间的本地凭据，跟你的 SaaS 解耦。

Token scope 由 SaaS 自己定义。建议字段：

```json
{
  "tenant_id": "cherry-prod-customer-42",
  "scopes": ["outbox:claim", "outbox:complete", "inbound:write"],
  "iat": 1730000000,
  "exp": null
}
```

**TTL 建议**：长期（无 exp 或 ≥ 365d）。租户把 token 写进 `wechat-use orchestrate` 的 launchd plist env，频繁刷新会增加运维成本。

租户吊销由 SaaS 端做（revocation list / token rotation）。

---

## 2. Outbox 状态机

```
                  ┌─────────────────────┐
                  │       pending       │ ← SaaS 写入 (POST or trigger)
                  └─────────┬───────────┘
                            │ Mac orchestrate claim
                            ▼
                  ┌─────────────────────┐
                  │      claimed        │ ← lease_until = now + 60s
                  └────┬────────────┬───┘
              done    │            │   fail (retryable)     fail (hard)
                      │            │
          ┌───────────┘            └───────────┐                ┌───────┐
          ▼                                    ▼                ▼       ▼
   ┌─────────────┐                  ┌──────────────────┐  ┌─────────────┐
   │    done     │                  │ pending          │  │   failed    │
   └─────────────┘                  │ (next_attempt_at │  └─────────────┘
                                    │  = now + backoff)│
                                    └──────────────────┘

Lease expiration:
  claimed AND lease_until < now
  → automatically reset to pending (visibility timeout)
```

**lease_until** 是关键：Mac 崩了 / 网断了，超过 lease 后 SaaS 端自动把 row reset 成 pending，下一个 claim 拿到。

**默认值**：
- lease 60 秒
- backoff 序列：30s / 2min / 10min / 30min（每次 ×4）
- max retry：5 次 → 转 `failed`

---

## 3. Endpoints

### 3.1 `GET /api/wechat-outbox/claim?limit=N`

**用途**：Mac orchestrate 请求 N 条任务，SaaS 原子 claim 把它们标 `claimed` 返回。

**Request**:
```http
GET /api/wechat-outbox/claim?limit=10
Authorization: Bearer <saas-token>
```

**Response 200**:
```json
{
  "rows": [
    {
      "id": "out_abc123",
      "idempotency_key": "cherry-order-1234-msg-1",
      "to": "wxid_xxxx OR room_id@chatroom OR display_name",
      "text": "您好，订单已确认...",
      "claimed_at": "2026-04-28T10:00:00Z",
      "lease_until": "2026-04-28T10:01:00Z",
      "attempt": 1
    }
  ]
}
```

**字段说明**：
- `id`：SaaS 内部主键，Mac 透传，回 done/fail 用同一个 id
- `idempotency_key`：**必填**。SaaS 跨 retry 用同一个 key（`cherry-order-${id}-msg-${seq}`），防止 Mac 重复 send 时重复扣
- `to`：recipient hint（wxid / room_id / 备注名 / 群名）。Mac 调本机 `/v1/send` 时透传，wechat-use 自己 resolve
- `lease_until`：超过此时间 SaaS 应自动 reset 这条为 pending

**Response 200 (no work)**：
```json
{ "rows": [] }
```

Mac 在收到空数组时按 poll interval 退避（1s → 2s → 5s 上限）。

**Response 401 / 403**：bearer 错或 scope 不够 → Mac orchestrate 报错并 sleep 30s 重试。

**Response 5xx**：SaaS 端故障 → Mac 重试。

---

### 3.2 `POST /api/wechat-outbox/:id/done`

**用途**：Mac 已经成功 send 完，回报。

**Request**:
```http
POST /api/wechat-outbox/out_abc123/done
Authorization: Bearer <saas-token>
Content-Type: application/json

{
  "send_message_id": "wechat-msg-server-id-xxx",
  "delivered_verified": true,
  "completed_at": "2026-04-28T10:00:35Z"
}
```

**字段说明**：
- `send_message_id`：wechat-use 返回的 server_id（可空，老版本不返回）
- `delivered_verified`：wechat-use 是否在 DB 看到落库（关键 —— 用来 dedupe）
- `completed_at`：完成时间

**Response 200**:
```json
{ "ok": true }
```

**幂等性**：相同 `id` 多次调 done 必须等价（SaaS 用第一次的 send_message_id，忽略后续）。Mac 网络抖动可能重复推。

---

### 3.3 `POST /api/wechat-outbox/:id/fail`

**用途**：Mac send 失败回报。

**Request**:
```http
POST /api/wechat-outbox/out_abc123/fail
Authorization: Bearer <saas-token>
Content-Type: application/json

{
  "error_code": "send_failed | resolve_failed | bridge_unavailable | wechat_blocked | hard_invalid",
  "error_message": "消息发送路径已执行...",
  "retryable": true,
  "failed_at": "2026-04-28T10:00:35Z"
}
```

**`error_code` 枚举**：

| code | retryable | 含义 | SaaS 行为建议 |
|---|---|---|---|
| `send_failed` | true / false | 发送失败或送达结果未确认；详细状态见 `error_message` | 仅 `retryable=true` 时退避重试；false 时转待核查，不能自动重发 |
| `resolve_failed` | false | 收件人 resolve 不到（wxid 错 / 不在通讯录） | 转 `failed`，通知运营 |
| `bridge_unavailable` | true | wechat-use REST :18402 不可达（daemon 没起 / Mac 网断） | 走 backoff 重试 |
| `wechat_blocked` | false | WeChat 提示账号风控（这条 send 永久无法成功） | 转 `failed`，紧急通知 |
| `hard_invalid` | false | payload 非法（text 为空 / 超长） | 转 `failed`，标 SaaS bug |
| `lease_expired` | true | Mac 自报 lease 已过期但仍想回报 | SaaS 接受并归档（不应 retry） |

发送动作执行过，不等于已确认送达。SaaS 必须读取每条回执的 `retryable`，不能仅凭 `error_code=send_failed` 重发。发送超时或连接中断后，原发送仍可能完成；核查会话前应保留任务记录，停止自动重发。

**Response 200**:
```json
{ "ok": true, "next_attempt_at": "2026-04-28T10:02:35Z OR null" }
```

`next_attempt_at` 让 Mac 知道下次什么时候这条会重新 claim 上（仅供日志）。

---

### 3.4 `POST /api/wechat-inbound`

**用途**：Mac 把 wechat-use SSE 入站事件 push 到 SaaS。

**Request**:
```http
POST /api/wechat-inbound
Authorization: Bearer <saas-token>
Content-Type: application/json
X-Wechat-Signature: t=1730000000,v1=<hex-hmac-sha256>
X-Idempotency-Key: <event_id>

{
  "event_id": "opaque-account-scoped-event-id",
  "account_id": "source-wechat-account",
  "ts": "2026-04-28T10:00:00Z",

  "normalized": {
    "conversation_id": "wxid_xxx OR group_id@chatroom",
    "is_group": true,
    "sender_wxid": "wxid_caller",
    "sender_display_name": "张三",
    "message_kind": "text | image | voice | video | file | url | quote | recall | system | mp | contact | location | emoticon | chat_history | forward | transfer | red_envelope | unknown",
    "text": "@bot 你好",
    "is_mentioned": true,
    "mentioned_ids": ["my_bot_wxid"],
    "from_self": false
  },

  "raw_event": {
    "...": "wechat-use SSE payload v1.10.28 schema 全字段透传"
  }
}
```

**两层字段**：
- `normalized`：常用字段。保留 `voice`、`file`、`mp`、`recall` 等既有名称；特殊类型会保留各自名称。
- `raw_event`：daemon 原始消息行，使用 snake_case，保留 `local_id`、`server_id`、`message_kind`、`mentioned_ids`、`url_link`、`mini_program`、`refer`、`recall`、`media` 等字段。
- `account_id`：来源微信账号。新版模板也将其保存在原始记录中，旧接收端可以忽略这个可选字段。

`event_id` 是不透明的幂等标识，不应按整数解析。账号隔离、没有 server ID 的消息和好友事件都使用稳定标识；重试保持相同标识和消息正文。

租户优先用 `normalized`；要做特殊处理（媒体识别 / quote 链 / recall）查 `raw_event`。

**Response 200**:
```json
{ "ok": true }
```

**幂等性**：SaaS **必须** dedupe by `event_id`（或 `X-Idempotency-Key`）。Mac 网络抖动 / SaaS 5xx retry 时同一 event 会重推。

**Response 4xx**：401/403、408、425、429 保留并重试；其他 4xx 保留为 rejected，供检查和显式重试。

**Response 5xx / 网络错误**：Mac 持久化重试，不因经过 30 分钟而丢弃。支持 Retry-After 秒数或日期。重定向不会被跟随。

---

## 4. Webhook 签名（防伪 + 防重放）

Mac 推 `/api/wechat-inbound` 时签：

```
sig_payload = "<unix_timestamp>.<request_body_string>"
v1 = HMAC-SHA256(saas-side webhook_secret, sig_payload)
header: X-Wechat-Signature: t=<unix_timestamp>,v1=<hex>
```

`webhook_secret` 是 SaaS 在配置租户 token 时同时下发的（独立 secret，不复用 bearer）。

SaaS 端验证：
1. parse `t` + `v1`
2. abs(now - t) > 5 min → reject（防重放）
3. recompute v1 with stored secret → 不 match → reject

参考 Stripe / Slack webhook 签名模式。

---

## 5. 错误处理 / 重试 / 退化

### Mac orchestrate 行为

| 场景 | Mac 行为 |
|---|---|
| outbox claim 返回 5xx / 网络错 | 指数 backoff（1s → 2s → 5s → 30s），不停重试 |
| outbox claim 返回 401/403 | 立刻 sleep 60s + log 报警（token 错 → 等用户介入） |
| 原生发送前未就绪 | 保存可重试回执，SaaS 确认并重新分配任务后重试 |
| 原生发送开始后超时 / 结果未确认 | 保存不可自动重试的待核查回执，不再发送同一任务 |
| webhook POST 5xx / 网络失败 | SQLite 持久重试；保留队列上限 10,000 条，满后暂停接收，不驱逐旧记录 |
| webhook POST 4xx | 鉴权、超时和限流错误重试；其他错误保留为 rejected |
| ACK 发送失败或 worker 重启 | 从持久回执恢复；重复领取已完成任务时重放回执，不重发微信 |

本地队列检查与恢复见 [持久队列说明](orchestrate-queue.html)。Orchestrate 在支持 event_page 的 daemon 上按账号及分片持久游标补读断线事件，入队与游标更新同时提交；首次历史只建立去重记录。数据库替换后无法确认身份的历史保留待检查，真实环境持续运行仍待验收。

### SaaS 端必做（spec 强制）

- Outbox claim 是**原子的**（单条 SQL：`UPDATE bot_outbox SET status='claimed', lease_until=? WHERE id IN (SELECT ... LIMIT N AND status='pending') RETURNING *`）
- `done` / `fail` 幂等（同 id 多次调结果等价）
- inbound `event_id` 主键 dedup
- 后台 cron / queue worker 周期性把 `claimed AND lease_until < now` 重置为 `pending`（visibility timeout）

---

## 6. 实现 checklist（SaaS 端）

最小可用接入：

- [ ] D1/Postgres `bot_outbox` 表 + 索引（`idx_status_next_attempt`）
- [ ] `bot_inbound_events` dedupe 表（PK `event_id`）
- [ ] 4 个 endpoint 实现（含 idempotency / lease）
- [ ] bearer token 鉴权 middleware（per-tenant scope）
- [ ] webhook signature 验证（HMAC-SHA256 + 5min replay window）
- [ ] cron 重置 lease 过期的 claimed → pending
- [ ] 监控：未 claim 的 pending 堆积告警 / 5xx error 比例告警

参考实现：
- TypeScript / Hono / D1：[cherry-api 实现](https://github.com/cherryport/cherry/tree/main/services/api/src) (private repo, ask for access)
- 我们计划出 reference Worker template（v1.12 release）

---

## 7. 跟 v1.11.1 tunnel 的关系

| 用法 | 选哪个 |
|---|---|
| 我有持续 inbound 客户对话 + 异步 send 队列（cherry / 客服 bot / SaaS 集成） | **v1.12 orchestrate** ⭐ |
| 我 CF Worker 偶尔触发，要立刻发条微信，没有 inbound | v1.11.1 tunnel（同步直连，亚秒级） |
| 我同时要两种 | 都装：tunnel + orchestrate 同进程不冲突 |

v1.11.1 tunnel **不会** deprecate，但默认推荐路径是 v1.12 orchestrate。

---

## 8. 路线图 / TODO

- [ ] OpenAPI 3 spec 文件（codegen 友好）
- [ ] 契约测试套件（pytest / vitest，租户跑 → 通过即接入合格）
- [ ] reference SaaS server stub（CF Worker template + Vercel template）
- [ ] Mac `wechat-use orchestrate` Rust 实现（v1.12 release）
- [ ] webhook 重放窗口配置化（默认 5min）
- [ ] 多 SaaS 单进程模式（v1.13+）
- [ ] hosted outbox 选项评估（v1.13+，需要时再做）

---

## Changelog

- 2026-04-28 v1 草案：4 endpoint + 状态机 + 鉴权 + webhook 签名 + 错误码语义
